/** Centered landing content width — nav, Was ist Linker, and demo share these edges (680px × 1.5). */
export const LANDING_CONTENT_MAX_PX = 1020;

export const LANDING_CONTENT_CLASS = "mx-auto w-full max-w-[1020px]";

export const LIVE_DEMO_SECTION_ID = "live-demo";

/** Hero + footer display headlines — Denton Condensed, same scale. */
export const LANDING_DISPLAY_HEADLINE_CLASS =
  "font-retell-display text-[clamp(54px,6.25vw,120px)] font-normal leading-[0.88] tracking-[-0.02em] text-white";
