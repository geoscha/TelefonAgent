import type { DashboardStats } from "@/lib/types";

export const mockDashboardStats: DashboardStats = {
  callsToday: 12,
  avgDurationSeconds: 134,
  openSuggestions: 3,
  autoResolvedPercent: 67,
};
